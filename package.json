{
  "name": "space-aws",
  "version": "0.1.0",
  "private": true,
  "description": "SPACE AWS Chrome Extension (Manifest V3) - Region-aware AWS Console header theming",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "package": "bash scripts/package-extension.sh"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
